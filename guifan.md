# 项目规范文档 - 文章一键多发平台

> **重要说明**: 此文件是项目唯一最新规范源，所有新规范和规范更新都必须及时更新到此文件。此文件具有最高权威性。

---

## 1. 项目管理规范

### 1.1 沟通语言规范
- **强制要求**: 项目所有沟通和文档必须使用中文
- **适用范围**: 用户界面、错误信息、日志输出、代码注释、技术文档
- **例外情况**: 第三方API接口文档、技术术语无法翻译的情况

### 1.2 进度管理规范
- **进度记录**: 使用 `jindu.md` 文件进行流水账式记录
- **记录原则**: 只增不改，按时间顺序追加
- **记录内容**: 时间戳、任务、进展、责任人、状态
- **更新频率**: 每次项目有新进展都必须及时记录

### 1.3 规范管理规范
- **规范来源**: `guifan.md` 是唯一最新规范源
- **更新原则**: 任何新规范或规范更新都必须及时更新到此文件
- **权威性**: 此文件具有最高权威性，所有开发活动必须遵循

### 1.4 Git仓库管理规范
- **仓库地址**: https://github.com/xiaohuAI319/ArticlePosts
- **主分支**: main
- **访问权限**: 公开仓库
- **敏感信息保护**: 使用.gitignore保护敏感信息
- **提交规范**: 使用中文提交信息，格式规范
- **推送权限**: Claude Code助手不得主动执行git push命令，但用户明确指令时可以操作
- **协作原则**: 保护代码仓库安全，用户指令优先执行

### 1.5 文档Git管理规范
- **jindu.md**: 已添加到.gitignore，不提交到Git仓库
- **guifan.md**: 已添加到.gitignore，不提交到Git仓库，避免敏感信息泄露
- **constitution.md**: 核心文档，需要提交到Git仓库
- **project-management.md**: 记忆文档，需要提交到Git仓库
- **配置文件**: 所有敏感配置文件都在.gitignore保护范围内
- **敏感信息原则**:
  - 规范文档（guifan.md、jindu.md）可能包含敏感信息，一律不提交到Git仓库
  - 如需记录敏感配置，应使用config.json等专用配置文件
  - 所有敏感信息必须通过.gitignore保护，不得提交到Git仓库
- **文档同步策略**:
  - 重要规范更新通过其他方式（如会议记录、邮件通知）传达给团队
  - 规范版本管理通过本地文件维护，确保团队信息同步
  - 关键配置变更需要团队确认并记录

### 1.6 配置管理规范
- **API key管理**: 敏感API key存储在config.json中，通过.gitignore保护
- **配置读取**: 使用Electron IPC机制安全读取配置，避免直接暴露
- **环境隔离**: 开发环境和生产环境使用不同的配置策略
- **配置格式**: 使用JSON格式存储配置，支持嵌套键值访问
- **安全性**: 所有敏感信息（API key、凭据等）必须加密存储和传输
- **数据库位置**: SQLite数据库文件存储在 `src/data/app.db`，所有数据库操作都使用此路径

### 1.7 代码质量规范
- **调试信息**: 生产环境代码中不允许有console.log等调试输出
- **错误处理**: 对非关键错误采用静默处理，避免控制台噪音
- **API兼容**: 使用最新版本的API，避免废弃警告
- **时序处理**: 对异步操作要正确处理Promise和回调时序
- **资源清理**: 组件卸载时必须正确清理定时器和事件监听器
- **第三方提示**: React开发环境提示（如React DevTools）不属于代码错误，无需处理

### 1.8 依赖管理规范
- **禁止私自安装**: 绝对禁止私自运行 `npm install`、`npm install --force` 等大型依赖安装命令
- **必须通知**: 所有依赖安装操作必须通知用户手动完成
- **安全原则**: 保证T001-T010已开发功能正常运行是最高优先级
- **依赖检查**: 如需安装依赖，必须先确认不影响现有功能
- **执行命令**: 如需安装依赖，使用以下命令：
  - 标准安装: `npm install`
  - 强制重装: `npm install --force`
  - 清理缓存: `rm -rf node_modules/.cache`
- **原生模块编译**: Electron项目中的原生模块（如better-sqlite3）需要为Electron环境重新编译：
  - 标准编译: `npx electron-rebuild`
  - **强制重编译**: `npx electron-rebuild --force` (✅ 正确指令)
  - 清理后重编译: `rm -rf node_modules && npm install && npx electron-rebuild --force`
  - npm rebuild: `npm rebuild better-sqlite3` (仅适用于Node.js环境)
  - 编译环境: Node.js v22.19.0 (NODE_MODULE_VERSION 127) + Electron v28.3.3 (NODE_MODULE_VERSION 119) + better-sqlite3@9.6.0
  - **关键问题**: Electron内部使用Node.js NODE_MODULE_VERSION 119，开发环境使用NODE_MODULE_VERSION 127，必须用electron-rebuild为Electron环境编译

### 1.9 开发环境端口管理规范
- **固定端口**: 开发环境统一使用3000端口，避免频繁更换端口
- **服务持续性**: webpack-dev-server启动后应保持运行，支持热重载，非必要不重启
- **重启原则**: 只有在以下特殊情况下才重启3000端口：
  - 应用启动失败，出现`EADDRINUSE: address already in use :::3000`错误
  - webpack-dev-server进程处于僵尸状态（端口被占用但无法访问）
  - Electron应用无法连接到开发服务器
  - 依赖安装后需要重新初始化开发环境
- **禁止随意重启**: 正常开发过程中，即使有代码改动也不需要手动重启，webpack会自动热重载
- **端口冲突解决**: 仅在符合重启原则时使用PowerShell命令强制终止占用端口的进程
- **有效kill命令**:
  - 单进程终止: `powershell -Command "Stop-Process -Id <进程ID> -Force"`
  - 批量端口清理: `powershell -Command "Get-NetTCPConnection -LocalPort 3000 | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue }"`
- **失效命令**: 以下命令经测试无效，不应使用：
  - `pwsh -Command "Get-NetTCPConnection..."` (语法错误)
  - `taskkill /F /PID <ID>` (在Git Bash中无法正确执行)
  - `cmd.exe /c "taskkill..."` (执行不彻底)
- **执行时机**: 仅在确认符合重启原则时执行，避免不必要的重启操作
- **权限要求**: 使用Force参数确保能够终止占用端口的进程
- **验证方法**: 执行后使用`netstat -ano | findstr :3000`验证端口是否释放

---

## 2. 开发规范

### 2.1 技术架构规范
- **前端技术**: 必须支持富文本编辑，兼容飞书文档格式
- **自动化技术**: 必须使用Chrome MCP进行可视化操作
- **平台集成**: 每个平台（知乎、小红书、百家号、头条号、知识星球、掘金、CSDN）必须有独立模块
- **数据安全**: 所有敏感信息必须加密存储

### 2.2 代码规范
- **注释规范**: 所有函数和复杂逻辑必须用中文注释
- **变量命名**: 优先使用中文拼音或英文，保持一致性
- **错误处理**: 所有错误信息必须用中文清晰描述
- **日志规范**: 所有日志输出必须用中文，包含足够上下文信息

### 2.3 测试规范
- **测试驱动**: 所有功能必须先写测试，后写实现
- **测试覆盖率**: 核心功能测试覆盖率必须达到100%
- **集成测试**: 每个平台集成必须通过完整的集成测试
- **用户场景测试**: 必须模拟真实用户使用场景
- **测试类型**: 单元测试、集成测试、端到端测试、性能测试
- **测试框架**: 使用Jest作为主要测试框架
- **测试报告**: 每个模块完成后必须有详细的测试报告

### 2.4 应用架构规范
- **Electron架构**: 主进程和渲染进程严格分离，安全通信
- **React版本**: 必须使用React 18+，启用并发特性
- **状态管理**: 使用Redux Toolkit进行状态管理，支持持久化
- **组件库**: 使用Ant Design作为主要UI组件库
- **CSS方案**: 使用CSS-in-JS或CSS模块化方案
- **构建工具**: 使用Webpack进行模块打包和开发服务器

### 2.5 组件开发规范
- **组件结构**: 每个组件必须有独立的文件夹结构
- **组件命名**: 使用PascalCase命名，语义化命名
- **Props验证**: 必须进行Props类型验证和默认值设置
- **状态管理**: 组件内部状态使用Hooks管理
- **样式规范**: 组件样式必须与主题系统集成
- **错误边界**: 关键组件必须有错误边界处理
- **性能优化**: 使用React.memo和useMemo进行性能优化

---

## 3. 产品规范

### 3.1 用户体验规范
- **界面语言**: 所有界面元素必须使用中文
- **操作反馈**: 每个操作必须有清晰的中文反馈
- **错误提示**: 错误信息必须用中文描述，并提供解决方案
- **帮助文档**: 所有帮助文档必须用中文编写

### 3.2 功能规范
- **富文本编辑**: 必须支持飞书文档的所有格式特性
- **一键多发**: 点击按钮后能够同时发布到多个平台
- **登录管理**: 支持扫码登录，记住登录状态
- **配置管理**: 所有平台配置项必须可视化，支持"不需要"选项

### 3.3 平台支持规范
- **支持平台**: 知乎、小红书、百家号、头条号、知识星球、掘金、CSDN
- **平台适配**: 每个平台的特殊要求必须适配
- **内容转换**: 自动适配不同平台的内容格式要求
- **发布状态**: 实时显示每个平台的发布状态

---

## 4. 安全规范

### 4.1 数据安全规范
- **加密存储**: 用户名密码、token等敏感信息必须加密
- **传输安全**: 所有网络传输必须使用HTTPS
- **访问控制**: 严格限制对用户数据的访问权限
- **审计日志**: 所有操作必须记录审计日志

### 4.2 隐私保护规范
- **数据最小化**: 只收集必要的用户数据
- **用户知情**: 必须明确告知用户数据用途
- **数据删除**: 提供用户数据删除功能
- **第三方共享**: 未经用户同意不得与第三方共享数据

---

## 5. 部署规范

### 5.1 环境规范
- **开发环境**: 本地开发环境配置规范
- **测试环境**: 与生产环境一致的测试环境
- **生产环境**: 高可用、安全的生产环境
- **数据备份**: 定期数据备份和恢复测试

### 5.2 发布规范
- **版本管理**: 使用语义化版本号
- **发布流程**: 测试→预发布→生产的标准发布流程
- **回滚机制**: 必须有快速回滚机制
- **监控告警**: 完善的监控和告警机制

---

## 6. 文档规范

### 6.1 技术文档规范
- **API文档**: 详细的API接口文档
- **架构文档**: 系统架构设计文档
- **部署文档**: 详细的部署和运维文档
- **故障排查**: 常见问题和故障排查手册

### 6.2 用户文档规范
- **用户手册**: 详细的用户操作手册
- **FAQ**: 常见问题解答
- **视频教程**: 关键操作的视频教程
- **在线帮助**: 应用内帮助系统

---

## 7. 更新记录

### 2025-10-14 v1.0.0
- 初始版本发布
- 建立项目管理、开发、产品、安全、部署、文档六大规范体系
- 确立中文优先的开发原则
- 制定进度追踪和规范管理机制

### 2025-10-14 v1.1.0
- 添加Git仓库管理规范
- 建立GitHub远程仓库：https://github.com/xiaohuAI319/ArticlePosts
- 配置.gitignore保护敏感信息
- 确立jindu.md和guifan.md的Git管理规则
- 完善文档Git管理规范

### 2025-10-14 v1.2.0
- 添加TDD测试驱动开发规范
- 建立测试覆盖率和质量标准
- 完善技术架构实施细节
- 添加Electron安全和性能规范

### 2025-10-14 v1.3.0
- 添加应用架构实施规范
- 建立组件开发和状态管理规范
- 完善HTTP API设计和安全规范
- 添加界面响应式设计规范
- 制定应用发布和部署流程

### 2025-10-15 v1.4.0
- 添加开发环境端口管理规范
- 建立固定端口使用原则（统一使用3000端口）
- 制定pwsh命令处理端口冲突的标准流程
- 完善开发环境自动化管理机制

### 2025-10-15 v1.5.0
- 优化端口重启原则，明确禁止随意重启3000端口
- 建立服务持续性原则，webpack-dev-server应保持运行支持热重载
- 定义重启的四种特殊情况，避免不必要的重启操作
- 提高开发效率，减少端口管理时间浪费

### 2025-10-15 v1.6.0
- 验证并确定有效的进程终止命令
- 明确标注失效的kill命令，避免重复尝试
- 建立PowerShell为标准的进程管理工具
- 添加端口释放验证方法，确保操作有效性

### 2025-10-15 v1.7.0
- 修复章节编号重复问题，将端口管理规范调整为1.9
- 验证实际有效的PowerShell端口清理命令
- 确认单进程终止命令的有效性：`powershell -Command "Stop-Process -Id <进程ID> -Force"`
- 验证端口状态检查命令：`netstat -ano | findstr :3000`
- 建立实际验证过的进程管理流程

### 2025-10-15 v1.8.0
- 添加Git推送权限规范：Claude Code助手不得执行git push命令
- 建立协作原则：所有推送操作需经用户确认后手动完成
- 保护代码仓库安全，明确推送权限边界

### 2025-10-15 v1.9.0
- 完成T013二维码生成和显示功能开发
- 建立完整的扫码登录基础架构
- 支持多平台登录状态管理和会话跟踪
- 集成Redux状态管理和IPC通信机制

### 2025-10-15 v1.10.0
- 优化Git推送权限规范：明确Claude Code助手权限边界
- 建立用户指令优先执行原则
- 保护代码仓库安全的同时提升协作效率

### 2025-10-15 v1.11.0
- 完成T014 Puppeteer浏览器集成开发
- 建立完整的浏览器自动化基础架构
- 支持17项反检测参数和隐身模式配置
- 集成6个核心IPC API接口
- 创建70+测试用例的TDD测试套件
- 为后续平台自动化登录奠定坚实基础

### 2025-10-15 v1.12.0
- 解决T014浏览器创建双窗口阻塞问题
- 修复Chrome路径检测导致的额外进程启动
- 优化Chrome启动参数，确保单实例运行
- 完善浏览器生命周期管理和自动清理机制
- 验证Puppeteer隐身模式的完整功能
- 建立稳定的浏览器自动化测试环境

### 2025-10-15 v1.13.0
- 完成T015平台自动化登录服务开发
- 创建PlatformAutoLoginService.js核心服务，集成T012/T013/T014服务
- 实现完整的QR码登录自动化流程，支持扫码状态监控和会话创建
- 创建LoginTestPage.jsx测试界面，提供完整的登录测试功能
- 建立完整的TDD测试套件，17个测试用例覆盖所有核心功能
- 集成4个核心IPC API接口，支持渲染进程与主进程安全通信
- 修复导航问题，将登录测试页面正确添加到应用界面
- 为后续T016+平台发布自动化奠定坚实基础

---

## 8. 规范更新流程

1. **规范提出**: 任何人都可以提出规范改进建议
2. **讨论评审**: 团队讨论规范的合理性和可行性
3. **更新发布**: 更新 `guifan.md` 文件
4. **通知执行**: 通知团队成员执行新规范
5. **监督检查**: 定期检查规范执行情况
6. **Git记录**: 重要规范更新要提交到Git仓库

---

**重要提醒**: 此文档是项目的规范权威，所有开发活动必须严格遵守。如有疑问或建议，请及时提出讨论更新。