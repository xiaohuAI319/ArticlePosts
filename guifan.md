# 应用核心发布流程开发规范 (V1.0)

本规范旨在定义文章发布功能的核心逻辑与交互流程，作为后续开发的唯一标准。

## 核心流程

### 1. 文章编辑页UI与交互

-   **功能要求**：在文章编辑界面的右侧，必须展示一个“平台状态控制面板”。
-   **面板内容**：对于系统支持的每一个平台，都需展示以下三个维度的信息：
    1.  **平台名称**：如“知乎”、“掘金”。
    2.  **启用状态**：使用一个开关（Toggle）控件表示。用户可以随时点击该开关，实时启用或禁用该平台。此操作将**立即**更新数据库中的对应状态。
    3.  **登录状态**：显示为“已登录”或“未登录”的文本状态。
-   **交互要求**：
    -   如果平台为“未登录”状态，该文本必须是可点击的。点击后，触发该平台的登录流程（详见第2点）。

### 2. 交互式登录流程

-   **触发**：用户点击平台状态面板中的“未登录”按钮。
-   **执行**：
    1.  应用主进程调用对应平台模块的 `showLoginWindow()` 函数。
    2.  弹出一个独立的、用于用户扫码或输入账号密码登录的窗口。
    3.  应用需监听该窗口的登录成功事件。
-   **数据同步**：
    1.  一旦用户登录成功，应用必须立即从登录窗口中捕获最新的有效Cookie。
    2.  将获取到的新Cookie**立即**更新到数据库中，覆盖旧的记录。
    3.  通知UI刷新，将对应平台的登录状态从“未登录”更新为“已登录”。
    4.  此更新需保证【平台管理】页面与此处的逻辑完全同步。

### 3. “一键发布”执行逻辑

-   **触发**：用户在文章编辑页点击“一键发布”按钮。
-   **执行步骤**：
    1.  **筛选平台**：应用首先从数据库中查询出所有被用户标记为“已启用” (`is_enabled = true`) 的平台。
    2.  **验证登录**：对上一步筛选出的平台列表，逐一进行最终的登录状态检查（调用 `checkLogin()` 函数）。
    3.  **创建任务队列**：只将那些“已启用”且“已登录”的平台，加入到本次的最终发布任务队列中。
    4.  **执行发布**：遍历任务队列，对每个平台执行文章发布操作。所有被用户禁用或登录失效的平台都将被自动跳过。

### 4. 发布结果反馈

-   **过程追踪**：在发布过程中，应用需记录下每个平台的发布结果（成功或失败）。
-   **结果展示**：当所有平台的发布任务都执行完毕后，必须弹出一个总结对话框，清晰地向用户展示本次发布任务中，哪些平台发布成功，哪些平台发布失败。

## 数据库与模块要求

-   **平台表（`platforms`）**：必须包含一个用于标识启用状态的字段，如 `is_enabled` (布尔类型)。
-   **平台发布模块（`publisher/*.js`）**：每个平台模块必须标准化，至少导出以下三个函数：
    1.  `checkLogin(cookie)`: 检查给定的cookie是否有效，返回布尔值。
    2.  `showLoginWindow()`: 创建并管理一个登录窗口，成功后返回新的cookie。
    3.  `publish(article, cookie)`: 执行文章发布，返回包含成功/失败状态的结果对象。